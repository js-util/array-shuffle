{
	"name": "@js-util/array-shuffle",
	"version": "1.0.0",
	"description": "Given an array, shuffle its content randomly. Note that the original array is not affected (Fisher–Yates method)",

	"keywords": [
		"array",
		"shuffle",
		"Fisher–Yates"
	],

	"main": "arrayShuffle.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},

	"author": "picocreator",
	"license": "MIT",
	"private": false,

	"repository": {
		"type": "git",
		"url": "https://github.com/js-util/arrayShuffle.git"
	},
	
	"engines": {
		"node": ">=8.0.0"
	}
}
